# 변경 이력

설계 판단의 근거는 커밋 메시지에 있습니다. 여기에는 **설치한 사람이 알아야 할 것**만
적습니다 — 무엇이 새어나갔었고 지금은 막혔는가.

## 0.2.0

0.1.0 은 자체 테스트 전부가 통과하는 상태로 게시됐습니다. 그 뒤 Claude Opus 와
Codex GPT-5.4 를 서로 격리해 여섯 라운드 이중 리뷰를 돌렸고, 매 라운드 두 리뷰어가
모두 실제 유출 경로를 찾아냈습니다. 이 판은 그 결과입니다.

**0.1.0 을 쓰고 있다면 올리는 편이 좋습니다.** 아래 유출 경로가 그 판에 전부
열려 있습니다.

### 막은 유출 경로

- **본문 마스킹을 뒤집었습니다.** "여기와 여기를 가린다"는 자리 목록은 화이트리스트처럼
  보여도 블랙리스트입니다 — 아는 자리만 가린다는 것은 모르는 자리는 샌다는 뜻입니다.
  이제 모든 값을 가리고 예외만 둡니다. 그 전까지 Anthropic 의 `document` 블록(문서
  첨부의 정식 경로), `tools[].description`, `metadata.user_id`, OpenAI 의
  `prediction.content`, `messages[].name`, 최상위 `user`, `file.file_data` 가
  원문 그대로 나갔습니다.
- **숫자와 사전의 키도 훑습니다.** `{"rrn": 8803121000068}` 처럼 숫자형으로 들어온
  식별자가 그대로 나가던 자리입니다.
- **예외 판정 근거를 요청자 바깥으로 옮겼습니다.** `type: "thinking"`, `type: "base64"`,
  `data:` 접두사처럼 요청 본문에 그냥 적으면 되는 신호를 방아쇠로 쓰면 그것은 예외
  조건이 아니라 우회 스위치입니다. 이제 출처(우리가 내보낸 서명인가)와 값(실제로
  디코드되고 매직 넘버가 맞는가)만 봅니다.
- **한 번 가린 값은 문맥이 바뀌어도 계속 가립니다.** 복원이 값을 탐지기가 알아볼 수
  없는 문맥으로 옮겨 놓아, 다음 턴에 대화 기록이 재전송될 때 앵커 없이 새어나갔습니다.
- **모르는 경로를 차단합니다.** 기본값이 통과였습니다. 경로는 점 구간(`..`)을 접은 뒤
  구분자까지 보고 비교합니다.
- **우회 표기를 부류로 막습니다.** 보이지 않는 문자를 목록으로만 막으면 언제나 공격자보다
  늦습니다. 서식 제어 문자(Cf)와 폭 0 결합 문자(Mn)를 범주째 걷어냅니다.
- **요청 헤더도 검사합니다.** 헤더에 개인정보가 있으면 요청을 거부합니다.

### 추가

- **OpenAI 호환 경로** (`/v1/chat/completions`). `OPENAI_BASE_URL` 로 붙습니다.
- **OpenAI Responses 경로** (`/v1/responses`). **Codex CLI가 쓰는 경로입니다** —
  OpenAI가 2026년 2월 Codex에서 chat/completions 지원을 완전히 제거해, 이 경로가
  없으면 Codex는 수문장을 통과할 수 없습니다. 실제로 물려 보고 404를 받아
  알았습니다. 붙인 뒤 진짜 Codex CLI 0.144.1로 두 턴을 돌려, 도구가 읽은
  개인정보 8종 13건이 전부 가려지고 화면에는 원문이 돌아오는 것을 확인했습니다
  (업스트림은 로컬 대역 서버 — 등급은 README 실환경 검증 기록 참조).
- **`goldenset-gaps`** — 못 잡는다고 선언한 것들을 점수로 공표하는 셋.
- **`sumunjang report --min-recall/--min-precision`** — 공표 수치를 CI 회귀 게이트로.

### 변경

- **세션 상한(1만 건)에 닿으면 요청을 거부합니다.** 오래된 값을 버리던 방식은 버린 값이
  다음 턴에 다시 가려지지 않아 그대로 유출됐습니다. 대화 기록은 매 턴 재전송되므로
  퇴출은 과거를 지우는 게 아니라 보호를 푸는 것이었습니다.
- **스트리밍은 업스트림에 통짜로 요청한 뒤 클라이언트에게만 흘려보냅니다.** 조각난
  응답에서 복원하면 청크 경계에서 깨집니다.

### 알려진 한계

새로 드러난 것을 포함해 [README 의 한계](README.md#한계)에 적었습니다. 못 잡는 것은
`goldenset-gaps` 에 정답으로 박아 두고 점수로 공개합니다.

## 0.1.0

첫 공개. 탐지 10종, Anthropic Messages 경로, `mask`·`scan`·`report` CLI.

**이 판에는 위의 유출 경로가 열려 있고 OpenAI 경로가 없습니다.** README 가 공표한
`goldenset-gaps` 재현율도 재현되지 않습니다(0.765 · 공표 0.778). 쓰지 마십시오.
